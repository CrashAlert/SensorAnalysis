from backend.tables import User, SensorSession, SensorData

def store_session(name, data):
    # find respective user
    # add session and for each row in csv add sensor data
    return

def get_session_as_json(name):
    # find session by name
    # get each related sensor data row
    return